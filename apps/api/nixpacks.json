{
  "providers": ["..."],
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_24", "pnpm"]
    },
    "install": {
      "cmds": ["pnpm install --frozen-lockfile"]
    },
    "build": {
      "cmds": ["pnpm run build"]
    },
    "start": {
      "cmd": "node dist/main.js"
    }
  }
}

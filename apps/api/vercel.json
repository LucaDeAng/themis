{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install && cd apps/api && pnpm run build",
  "devCommand": "pnpm run start:dev",
  "installCommand": "cd ../.. && pnpm install && pnpm --filter @themis/api exec prisma generate",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
